##isOdd
>Checks if a number is odd.

####Syntax
```js
isOdd(value)
```

- <code>value {Number}</code>

The number to check if odd.

####Example
```js
isOdd(0); //=> false
isOdd(2); //=> false
isOdd(3); //=> true
isOdd(5); //=> true
```